---
import BaseLayout from '../../layouts/BaseLayout.astro';
import BreadcrumbNav from '../../components/BreadcrumbNav.astro';
import { siteConfig } from '../../data/site';
import { generateBreadcrumbs } from '../../data/schema';
import { t, localizePath } from '../../i18n/utils';

const locale = 'en';
const { legal } = siteConfig;

const breadcrumbs = [
  { label: t('breadcrumb.home', locale), href: localizePath('/', locale) },
  { label: t('nav.impressum', locale) },
];

const breadcrumbSchema = breadcrumbs.map((b) => ({
  name: b.label,
  url: b.href ? `${siteConfig.url}${b.href}` : `${siteConfig.url}/en/legal-notice`,
}));
---

<BaseLayout
  title={`Legal Notice | ${siteConfig.name}`}
  description={`Legal notice (Impressum) for ${siteConfig.name}.`}
  noindex={true}
  locale="en"
>
  <BreadcrumbNav items={breadcrumbs} />

  <article class="py-12 md:py-16 lg:py-20">
    <div class="max-w-3xl mx-auto px-4 md:px-6">
      <h1 class="font-heading text-3xl md:text-4xl text-text mb-8">Legal Notice</h1>

      <div class="prose prose-lg max-w-none">
        <div class="bg-bg-alt border border-border rounded-card p-6 mb-8">
          <p class="text-text-muted text-sm mb-2">
            <strong>Note:</strong> As required by German law (§ 5 TMG), the legally binding Impressum is available in German.
          </p>
          <a
            href="/impressum"
            class="inline-flex items-center text-sm font-medium text-accent hover:text-accent-dark transition-colors"
          >
            View Impressum (German)
            <svg class="w-4 h-4 ml-1" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>

        <h2>Information according to § 5 TMG</h2>
        <p>
          {legal.businessName || siteConfig.founder.name}<br />
          {legal.responsiblePerson || siteConfig.founder.name}<br />
          Email: <a href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a>
        </p>
      </div>
    </div>
  </article>
</BaseLayout>
